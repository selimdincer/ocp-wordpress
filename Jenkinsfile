pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        echo "building the wordpress application"
      }
    }
     stage("test") {
      steps {
        echo "testing the wordpress application"
      }
    }
    stage("deploy") {
      steps {
        echo "deploying the wordpress application"
      }
    }
  }
}